import React from "react";
import Container from "../Common/Container";

interface Props {}

const CTA = ({}: Props) => {
  return <Container className="my-10 z-10">CTA</Container>;
};

export default CTA;
